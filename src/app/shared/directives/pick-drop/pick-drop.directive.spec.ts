import { PickDropDirective } from './pick-drop.directive';

describe('PickDropDirective', () => {
  it('should create an instance', () => {
    const directive = new PickDropDirective();
    expect(directive).toBeTruthy();
  });
});
